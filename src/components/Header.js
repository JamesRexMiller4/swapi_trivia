import React from 'react';
import './Header.scss';

const Header = ({name, quote, ranking}) => {
  return (
    <header className="header">
      <h2></h2>

    </header>
  )
}

export default Header;